/**

@def MARKER

@ref ::obj "Object" fields and flags used by this type.

<table>
<tr>
    <th>Field/Flag</th>
    <th>Explanation</th>
</tr>
<tr>
    <td>living::hp</td>
    <td>How many times the marker will work. If zero, infinite, otherwise
        every time the marker grants a mark, the value is decremented and
        when reached zero, the marker is removed.</td>
</tr>
</table>
*/
