diff --git a/src/curl.mk b/src/curl.mk
--- a/src/curl.mk
+++ b/src/curl.mk
@@ -21,6 +21,7 @@
 define $(PKG)_BUILD
     cd '$(BUILD_DIR)' && $(SOURCE_DIR)/configure \
         $(MXE_CONFIGURE_OPTS) \
-        --with-schannel \
+        --with-openssl='$(PREFIX)/$(TARGET)' \
+        --without-schannel \
         --with-libidn2 \
         --enable-sspi \
